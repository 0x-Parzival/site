export interface MiracleEvent {
  name: string;
  description: string;
  canto: string;
}

export interface CosmologyFeature {
  feature: string;
  puranic: string;
  modern: string;
  reconciliation: string;
}

export interface EthicalDilemma {
  scenario: string;
  puranicPerspective: string;
}

export interface CantoSummary {
  number: number;
  title: string;
  summary: string;
  keyThemes: string[];
} 